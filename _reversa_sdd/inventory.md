# Project Inventory — gatomia

> Generated by the Reversa Scout (reconnaissance phase).
> Confidence scale: 🟢 CONFIRMED (read directly from code/config) · 🟡 INFERRED · 🔴 GAP (needs human validation).

## 1. Overview

| Attribute | Value | Confidence |
|-----------|-------|------------|
| Project name | `gatomia` | 🟢 |
| Version | `0.37.0` | 🟢 |
| Type | VS Code extension (Agentic Spec-Driven Development) | 🟢 |
| Primary language | TypeScript | 🟢 |
| Extension host target | `engines.vscode ^1.90.0`, `@types/vscode ^1.84.0` | 🟢 |
| Extension bundle | `dist/extension.js` (esbuild, `--target=node16`) | 🟢 |
| Webview | React 18 SPA bundled by Vite | 🟢 |
| Database | None (state persisted via VS Code workspaceState + JSON files) | 🟢 |
| Containerization | None (no Dockerfile / docker-compose) | 🟢 |

This is a dual-build VS Code extension: a Node.js extension host (`src/`, bundled with esbuild) plus a React webview SPA (`ui/`, bundled with Vite). The two communicate via the VS Code `postMessage` bridge.

## 2. Languages

| Language | Extensions | File count (src/ui/tests/scripts) | Confidence |
|----------|-----------|-----------------------------------|------------|
| TypeScript | `.ts`, `.tsx` | 624 (457 `.ts` + 167 `.tsx`) | 🟢 |
| CSS | `.css` | 12 | 🟢 |
| Markdown | `.md` | 7 | 🟢 |
| JSON | `.json` | 4 | 🟢 |
| JavaScript | `.js` | 2 | 🟢 |
| HTML | `.html` | 1 | 🟢 |

- Total source-ish files (src, ui/src, tests, scripts, assets, resources): **~650** 🟢
- Total git-tracked files (excluding `node_modules`, `dist`, `coverage`): **~2103** 🟢 (includes specs/, docs/, AI-tooling config)

## 3. Top-level structure

```
src/                  Extension host (TypeScript, esbuild)
  extension.ts        Main activation entry point
  constants.ts        Shared constants
  commands/           Command registrations (4 files)
  features/           Domain modules (see Modules section)
  panels/             Webview panels (8 files)
  prompts/            Markdown-compiled prompts (+ prompts/target)
  providers/          VS Code tree views & UI providers (22 files)
  services/           Core business logic (34 files; incl. acp/, welcome/)
  types/              Shared type definitions (3 files)
  utils/              Utilities & adapters (28 files)
ui/                   React webview SPA (Vite)
  index.html          Webview HTML host
  src/index.tsx       Webview entry point
  src/bridge/         postMessage bridge (vscode.ts)
  src/components/     Reusable UI components
  src/features/       Feature views (agent-chat, hooks-view, workflow-composer, ...)
  src/lib/            Libraries (markdown rendering)
  src/services/       Webview-side services
  src/stores/         Zustand stores (cloud-agent, devin, spec-explorer)
  src/utils/          Webview utilities
tests/                Vitest test suites (unit / integration)
scripts/              Build scripts (build-prompts.js, ...)
assets/, resources/   Static assets and bundled resources
specs/                Spec-Driven Development artifacts (SpecKit/OpenSpec)
docs/                 Project documentation
```

> Excluded from analysis: `node_modules`, `.git`, `.reversa`, `_reversa_sdd`, `dist`, `build`, `coverage`, and AI-tooling directories (`.claude`, `.agents`, `.windsurf`, `.kiro`, etc.).

## 4. Entry points

| Path | Role | Confidence |
|------|------|------------|
| `src/extension.ts` | Extension activation; registers commands, providers, services | 🟢 |
| `src/constants.ts` | Shared constants | 🟢 |
| `ui/src/index.tsx` | React webview SPA bootstrap | 🟢 |
| `ui/index.html` | HTML host for the webview bundle | 🟢 |

## 5. Build, test & quality scripts (`package.json`)

| Script | Command | Purpose |
|--------|---------|---------|
| `build` | `build-prompts && build:ext && build:webview` | Full build |
| `build:ext` | `esbuild src/extension.ts --bundle ... --outfile=dist/extension.js` | Bundle extension |
| `build:webview` | `npm --prefix ui run build` | Build React webview (Vite) |
| `build-prompts` | `node scripts/build-prompts.js` | Compile markdown prompts to TS |
| `watch` | `concurrently "tsc -w -p ./" "npm run watch:webview"` | Dev watch |
| `test` | `vitest --pool threads --run` | Run all tests |
| `test:coverage` | `vitest --pool threads --coverage` | Coverage report |
| `lint` / `format` / `check` | `ultracite lint` / `format` / `check` | Quality gates (Biome via ultracite) |
| `package` | `vsce package` | Produce VSIX |
| `install:all` | `npm install && cd ui && npm install` | Install root + ui deps |

## 6. Configuration files

`package.json`, `tsconfig.json`, `tsconfig.test.json`, `vitest.config.ts`, `vitest.setup.ts`, `biome.json`, `mise.toml`, `ralph.yml`, `skills-lock.json`, `ui/package.json`, `ui/vite.config.ts`, `ui/tsconfig.json` 🟢

## 7. CI/CD (`.github/workflows/`)

| Workflow | Likely purpose (🟡 inferred from name) |
|----------|----------------------------------------|
| `ci.yaml` | Continuous integration (lint, test, build) |
| `cd.yaml` | Continuous delivery |
| `release.yaml` | Release packaging/publishing |
| `tag-trigger-release.yaml` | Tag-triggered release |
| `version-bump.yaml` | Automated version bumping |

> 🟡 Purposes inferred from filenames; the Archaeologist/Architect phases will confirm by reading the workflow definitions.

## 8. Testing

| Attribute | Value | Confidence |
|-----------|-------|------------|
| Framework | Vitest `^4.0.18` (threads pool) | 🟢 |
| Support libs | `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom` | 🟢 |
| `*.test.ts(x)` files | 232 | 🟢 |
| `*.spec.ts(x)` files | 15 | 🟢 |
| Total test files | ~247 | 🟢 |
| Layout | `tests/unit/`, `tests/integration/`, plus colocated `*.test.ts` in `src/` and `ui/` | 🟢 |

## 9. Database (surface scan)

🟢 **No database detected.** No DDL, SQL, Prisma schema, or ORM models found. The "migration" matches in the tree refer to spec-system migration (`spec-kit-migration`) and cloud-agent provider migration (`cloud-agents/migration-service`), not data migrations. State is persisted via the VS Code workspaceState API and JSON files (e.g., `.vscode/gatomia/`). The `reversa-data-master` agent is not required for this project.

## 10. Modules identified (drives the Archaeologist phase)

### Extension host (`src/`)
1. `agent-chat` — `src/features/agent-chat`
2. `agents` — `src/features/agents`
3. `cloud-agents` — `src/features/cloud-agents`
4. `devin` — `src/features/devin`
5. `hooks` — `src/features/hooks`
6. `orchestration` — `src/features/orchestration`
7. `spec` — `src/features/spec`
8. `steering` — `src/features/steering`
9. `tasks` — `src/features/tasks`
10. `providers` — `src/providers` (22 files)
11. `services` — `src/services` (34 files; incl. `acp/`, `welcome/`)
12. `panels` — `src/panels` (8 files)
13. `commands` — `src/commands` (4 files)
14. `utils` — `src/utils` (28 files)
15. `prompts` — `src/prompts` (+ `prompts/target`)

### Webview (`ui/src/`)
16. `webview-agent-chat` — `ui/src/features/agent-chat` (+ related components)
17. `webview-spec-explorer` — `create-spec-view`, `create-steering-view`, `components/spec-explorer`, `components/forms`
18. `webview-hooks-view` — `ui/src/features/hooks-view`, `components/hooks`
19. `webview-orchestration` — `features/orchestration`, `features/workflow-composer`, `components/workflow`, `workflow-graph`, `kanban`
20. `webview-preview` — `features/preview`, `interactive-view`, `simple-view`, `components/preview`, `refine`
21. `webview-welcome` — `ui/src/features/welcome`, `components/cli-options`
22. `webview-shared` — `bridge/`, `stores/`, `lib/`, `services/`, `components/ui`, `components/cloud-agents`, `components/devin`, `utils/`

> 🟡 The webview module grouping is an inferred partition for analysis convenience; the underlying folders are confirmed 🟢. The grouping can be edited in `.reversa/plan.md` before the Archaeologist runs.

## 11. Spec organization suggestion

**Suggested granularity: `module`** 🟢 — top-level domain folders under `src/features/` are the dominant signal, mirrored by `ui/src/features/`. This matches the already-persisted decision in `.reversa/config.toml` (`feature-folder` layout).
