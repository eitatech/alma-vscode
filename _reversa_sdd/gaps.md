# Remaining Gaps — gatomia-vscode

> Generated by the Reviewer on 2026-05-29. Doc level: **completo**.
> All escalated questions in `questions.md` were **answered**. This file tracks what is *not yet fully
> specified* despite a decision, plus decided defects awaiting implementation. None of these block a
> faithful reimplementation of current behavior — they are forward-cycle work items.

---

## Decided, but not yet specified (true residual unknowns)

### G-A — `steering` constitution-validation rules
- **Decision:** implement real validation (no longer "always-true").
- **Unknown:** the concrete rules (required sections? non-empty principles? schema/semantic checks?).
- **Next step:** run a `/reversa-requirements` cycle to define the validation contract before implementing `ConstitutionManager.validateConstitution` (`constitution-manager.ts:74-77`).
- **Confidence:** intent 🟢 / rules 🔴.

### G-B — `utils` MCP tool→server metadata signal
- **Decision:** replace the name heuristic with a reliable server-of-origin signal.
- **Unknown:** the exact `vscode.lm.tools` field/shape that carries server origin, and the migration path off `correlateToolWithServer` (`copilot-mcp-utils.ts:374`) + the `other-tools` fallback.
- **Next step:** verify the `vscode.lm` API surface for a server-origin field; spec the replacement.
- **Confidence:** intent 🟢 / implementation detail 🟡.

---

## Decided, pending implementation (known defects / debt with an agreed fix)

> These are **not** understanding-gaps — the specs describe current behavior accurately and the target is decided.
> Listed so a forward-cycle planner can pick them up. Cross-reference `traceability/spec-impact-matrix.md` §5.

| ID | Item | Decided fix | Primary location |
|----|------|-------------|------------------|
| D-1 | Devin standalone path is legacy | Remove `DevinPollingService` + `devin-commands` wiring; `cloud-agents` canonical | `extension.ts:621-687`, `commands/devin-commands`, `panels/devin-progress-panel` |
| D-2 | Autonomous loop builds non-canonical session | Build via `AgentChatSessionStore.createSession` | `orchestration/autonomous-agent-loop.ts:76-86` |
| D-3 | Autonomous loop checks invalid `"error"` state | Use terminal-state set: `completed`→success, `{failed,cancelled,ended-by-shutdown}`→failure | `orchestration/autonomous-agent-loop.ts:140` |
| D-4 | CR→tasks generator is a mock | Wire to `/speckit.tasks` scoped to the CR | `spec/review-flow/tasks-dispatch.ts:76-134` |
| D-5 | Hook variable gating not enforced | Implement strict `validateVariables` (reject out-of-scope vars at save) | `template-variable-parser.ts:299-310` |
| D-6 | Progress pages missing from registry | Register `cloud-agent-progress` in `page-registry.tsx` | `ui/src/page-registry.tsx`, `panels/cloud-agent-progress-panel.ts` |
| D-7 | Kanban unmounted + `src/` import | Add a Kanban importer; mirror `NormalizedTask`/`ExecutionState` (R-X-6) | `ui/src/components/kanban/kanban-board.tsx:3-5` |
| D-8 | spec-explorer bypasses shared bridge | Migrate to `@/bridge/vscode` + `types.ts` contract mirror | `webview-spec-explorer/{spec-explorer-store.ts:2, services/spec-explorer.ts:100}` |
| D-9 | Dead duplicate hooks components | Remove `components/hooks/*` + `components/cli-options/*` (no live importer) | `ui/src/components/hooks/`, `ui/src/components/cli-options/` |

---

## 🟡 reconciliations noted (non-blocking)

- **Grace windows:** with `devin` legacy (D-1), the canonical policy is `cloud-agents`' time-based grace (5 min known-PR / 1 h unknown); `GRACE_CYCLES_AFTER_TERMINAL=6` retires with the standalone path.
- **Devin PR actions** (`pr-review-integration.ts`) open the browser rather than calling the API — current behavior is faithful; retire with D-1.
- **"Parallel" non-blocking hooks** run sequentially (`hook-executor.ts:884-895`) — documented as observed; no change requested.
- **Dual-keyed webview protocol** (`type` + `command`) — backward-compat; simplify only if older hosts are dropped.

---

> **Bottom line:** the reverse-engineered specs are a faithful, high-confidence (≈88%) description of the
> *current* system. Every escalated gap has a maintainer decision. The items above are the agreed
> forward-cycle backlog, not blockers to understanding or reimplementing today's behavior.
