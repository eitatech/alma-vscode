# Questions for Validation — gatomia-vscode

> Generated by the Reviewer on 2026-05-29. **All 10 gaps resolved with the maintainer (Italo) in chat.**
> Answer mode: **chat**. Doc level: **completo**.
>
> **How to read this:** the Reviewer returned to the source to resolve everything code-determinable.
> Each gap is split into **✅ Code-confirmed fact** (now 🟢) and **❓ Maintainer intent** (the decision).
> Reclassification history is in `confidence-report.md`; residual (decided-but-not-yet-specced) items are in `gaps.md`.

---

## Question 1 — Devin ownership overlap (polling) 🔴→✅ RESOLVED

**Context:** `devin` module (`devin-polling-service`, `devin-session-storage`) **and** `cloud-agents` (`agent-polling-service` + `adapters/devin-adapter`).
**Specs affected:** `devin/*`, `cloud-agents/*`, `panels/cloud-agent-progress-panel`, `traceability/spec-impact-matrix.md` §4–5.

**✅ Code-confirmed fact:** Both pollers are instantiated **and started** in `extension.ts` (`DevinPollingService` `:633`, `AgentPollingService` `:155`), with **separate storage**; `devin-adapter` borrows only Devin **credentials/API client**. Comment at `extension.ts:621`: *"progress view now in Cloud Agents"*.

**Question:** Is the standalone `devin` poller legacy, or partitioned by entry point?

**✅ Answered — DECISION: `devin` standalone is LEGACY.** `cloud-agents` is the canonical Devin path. The standalone `devin` poller and `devin-commands` wiring are slated for removal. `cloud-agents` grace policy (5 min/1 h) is canonical; `GRACE_CYCLES_AFTER_TERMINAL=6` is legacy.
**Reclassification:** `devin/polling-cycle`, `devin/pr-state-task-sync` → documented as 🟢 **legacy (deprecated, scheduled for removal)**.

---

## Question 2 — Autonomous loop session shape & completion 🔴→✅ RESOLVED

**Context:** `AutonomousAgentLoopService` (`orchestration/autonomous-agent-loop.ts`).
**Specs affected:** `orchestration/autonomous-task-loop/*`.

**✅ Code-confirmed fact:** `startTask` (`:76-86`) builds a non-canonical session; `handleRegistryChange` (`:140`) checks `lifecycleState !== "error"` — an invalid state (terminal set: `completed|failed|cancelled|ended-by-shutdown`).

**Question:** Real agent-chat session or prototype placeholder?

**✅ Answered — DECISION: spawn a REAL session** via `AgentChatSessionStore.createSession`. Success criterion = `completed`; failure = `{failed, cancelled, ended-by-shutdown}`. The `!== "error"` check is a **defect** to fix.
**Reclassification:** `autonomous-task-loop` intended contract = 🟢; the `"error"` check flagged as 🟢-confirmed **defect** (target behavior specified).

---

## Question 3 — Change-request → tasks producer is a mock 🔴→✅ RESOLVED

**Context:** `spec/review-flow/tasks-dispatch.ts:76-134`.
**Specs affected:** `spec/change-request-lifecycle/*`.

**✅ Code-confirmed fact:** `dispatchToTasksPrompt` is a mock (random latency, ~10% fail, hard-coded tasks).

**Question:** Intended real producer?

**✅ Answered — DECISION: the SDD `/speckit.tasks` command, scoped to the change request.** The CR-lifecycle FSM stays 🟢; task **content** generation is to be wired to `/speckit.tasks` (mock replaced).
**Reclassification:** `change-request-lifecycle` tasks producer 🔴 → 🟢 (intended producer known); current mock documented as interim.

---

## Question 4 — `hooks` `validateVariables` is a stub 🔴→✅ RESOLVED

**Context:** `TemplateVariableParser.validateVariables` (`template-variable-parser.ts:299-310`).
**Specs affected:** `hooks/*` (R-HK-5).

**✅ Code-confirmed fact:** Always valid; `availableFor` defined but not enforced; lenient runtime fallback (missing → empty string).

**Question:** Enforce gating or keep lenient?

**✅ Answered — DECISION: ENFORCE per-trigger gating.** A hook using a variable not provided by its trigger should be rejected (validation errors at save time). The lenient empty-string fallback is **not** the intended final contract.
**Reclassification:** R-HK-5 intended behavior = strict validation 🟢; current lenient stub documented as a known gap to close (in `gaps.md`).

---

## Question 5 — `steering` `validateConstitution` is a stub 🔴→✅ RESOLVED (rules TBD)

**Context:** `ConstitutionManager.validateConstitution` (`constitution-manager.ts:74-77`).
**Specs affected:** `steering/*`.

**✅ Code-confirmed fact:** Always returns `true` ("Placeholder for validation logic").

**Question:** Implement validation or accept always-true?

**✅ Answered — DECISION: IMPLEMENT real validation.** The always-true placeholder is not acceptable. **Concrete validation rules are to be defined in a forward-cycle spec** (residual item → `gaps.md`).
**Reclassification:** intent = validation required 🟢; specific rules remain 🔴 (deferred to a `/reversa-requirements` cycle).

---

## Question 6 — Unmounted prototypes / broken progress panels 🔴→✅ RESOLVED

**Context:** `webview-orchestration` (Kanban + progress pages), `webview-shared/page-registry.tsx`, `panels/{devin,cloud-agent}-progress-panel`.
**Specs affected:** `webview-orchestration/unmounted-prototypes/*`, `webview-shared/bootstrap-page-selection/*`, `panels/cloud-agent-progress-panel/*`.

**✅ Code-confirmed fact:** `page-registry.tsx` has 10 pages — `devin-progress`/`cloud-agent-progress` **absent** (panels render "Unknown page"); `KanbanBoard` has no importer and imports from `src/`.

**Question:** Ship or remove?

**✅ Answered — DECISION: SHIP & WIRE.** Register `cloud-agent-progress` (and, consistent with Q1, the devin-specific progress is superseded — wire the unified `cloud-agent-progress` page) in `page-registry.tsx`, add a Kanban importer, and replace the `src/` import in `kanban-board.tsx` with a contract mirror (R-X-6).
**Reclassification:** unmounted-prototypes target state 🟢 (ship); the missing-registry/`src`-import facts documented as defects to fix.

> Note: Q1 made the standalone `devin` path legacy — so the **`cloud-agent-progress`** unified page is the one to wire; the legacy `devin-progress` panel is removed with the rest of the standalone `devin` wiring.

---

## Question 7 — `webview-spec-explorer` R-X-6 drift 🔴→✅ RESOLVED

**Context:** `webview-spec-explorer` (`spec-explorer-store.ts:2`, `services/spec-explorer.ts:100`).
**Specs affected:** `webview-spec-explorer/*`.

**✅ Code-confirmed fact:** Imports spec types from `src/` + uses a separate `acquireVsCodeApi()` (`window.specExplorerVscode`).

**Question:** Intentional exception or migrate?

**✅ Answered — DECISION: MIGRATE to the shared bridge** (`@/bridge/vscode`) + a `types.ts` contract mirror, aligning with R-X-6 like every other webview module.
**Reclassification:** drift documented as a tracked **migration item** (target 🟢); current state is a confirmed deviation.

---

## Question 8 — Duplicate hooks components 🔴→🟢 RESOLVED

**Context:** `webview-hooks-view`.
**Specs affected:** `webview-hooks-view/*`.

**✅ Code-confirmed fact (resolved from code):** Live path = `features/hooks-view/components/trigger-action-selector.tsx` (754 LOC, via `hook-form.tsx:18`). `components/hooks/trigger-action-selector.tsx` (313 LOC) + `components/cli-options/*` have **no live importer** → dead.

**Question:** Confirm cleanup?

**✅ Answered — DECISION: SAFE TO REMOVE** the dead `components/hooks/*` + `components/cli-options/*` duplicates (in-progress Rule-of-Three consolidation).
**Reclassification:** 🔴 → 🟢 (live variant confirmed; dead set confirmed removable).

---

## Question 9 — `utils` MCP tool→server correlation heuristic 🔴→✅ RESOLVED

**Context:** `correlateToolWithServer` (`copilot-mcp-utils.ts:374`).
**Specs affected:** `utils/mcp-discovery/*`.

**✅ Code-confirmed fact:** Layered name heuristics with an `other-tools` fallback.

**Question:** Is the heuristic acceptable, or is there a reliable signal?

**✅ Answered — DECISION: USE THE METADATA SIGNAL.** A reliable server-of-origin signal from `vscode.lm.tools` should replace the name heuristics. The current heuristic is documented as **interim best-effort** (🟡); the metadata-based approach is the intended target (residual implementation item → `gaps.md`).

---

## Question 10 — Spec `009` absent from lineage 🟡→🟢 RESOLVED

**Context:** `specs/` (jumps `008` → `010`); `traceability/spec-impact-matrix.md` §3.

**✅ Answered — DECISION: `009` was ABANDONED** — started then dropped; the number was never reused. No spec content exists.
**Reclassification:** lineage matrix §3 `009` 🔴/🟡 → 🟢 (definitively explained: abandoned).

---

## Resolved during review (no question needed)

- **`commands` Q1 / `providers` Q1 — `package.json` wiring** 🔴→🟢: **89 commands**; view containers `gatomia-chat` + `gatomia` with 10 contributed views (`agentChat`, `agentChatPrimary`, `specExplorer`, `actionsExplorer`, `steeringExplorer`, `hooksExplorer`, `wikiExplorer`, `cloudAgents`, `runningAgents`, `orchestration`, `overview`). The progress panels are `WebviewPanel`s (not contributed views) — correctly absent from `contributes.views`.
- **`providers` Q2 — overview/scaffold views**: `gatomia.views.overview` **is** contributed and wired (`extension.ts:1667`); `simple`/`interactive` pages are registered in `page-registry.tsx` (live demo surfaces). Not dead.
