# Flowcharts — `utils` module

> Source: `src/utils/`. Confidence: 🟢 CONFIRMED unless noted.
> Generated by the Reversa Archaeologist (escavação phase).

## 1. SpecSystemAdapter.initialize — active-system resolution 🟢

```mermaid
flowchart TD
    A([initialize]) --> B{workspace folder?}
    B -- no --> B0[throw: no workspace]
    B -- yes --> C[load settings via ConfigManager]
    C --> D{settings.specSystem set & != auto & valid?}
    D -- yes --> E[system = preference]
    D -- no --> F[detectAvailableSpecSystems]
    F --> G{available.length}
    G -- ">1" --> H[QuickPick SpecKit vs OpenSpec]
    H --> H1{chosen?}
    H1 -- yes --> H2[system = choice + saveSettings] 
    H1 -- no --> H3[system = SpecKit default - not persisted]
    G -- "1 or 0" --> I[system = detectActiveSpecSystem]
    E --> J
    H2 --> J
    H3 --> J
    I --> J{system}
    J -- SpecKit --> K[specsPath=specs/, promptsPath=.specify/templates]
    J -- OpenSpec --> L[specsPath=openspec, promptsPath=prompts]
    K --> Z([config set])
    L --> Z
```

## 2. parseTasksContent — dual-format task parse 🟢

```mermaid
flowchart TD
    A([for each line]) --> B{phase header? - not in denylist}
    B -- yes --> B1[finalizeTask + start new group]
    B -- no --> C{inline `- [x] T### ...`?}
    C -- yes --> C1[finalizeTask + push inline task - checked=completed]
    C -- no --> D{header `### T1.1: Title`?}
    D -- yes --> D1[finalizeTask + start currentTask]
    D -- no --> E{inside currentTask?}
    E -- yes --> F[collect content; track Acceptance Criteria checkboxes]
    E -- no --> A

    subgraph fin [finalizeTask]
      G([finalize]) --> G1{acceptance items?}
      G1 -- all checked --> S1[completed]
      G1 -- some --> S2[in-progress]
      G1 -- none --> S3[not-started]
      G --> G2{explicit **STATUS** marker?}
      G2 -- COMPLETE --> S1
      G2 -- IN-PROGRESS --> S2
      G --> G3[extract priority/complexity regex] --> push[push to group]
    end
```

## 3. MCP discovery + tool→server correlation 🟢

```mermaid
flowchart TD
    A([queryMCPServers]) --> B{vscode.lm.tools available?}
    B -- no --> B0[return empty]
    B -- yes --> C[loadMCPConfig from mcp.json]
    C --> D[for each lm tool: correlateToolWithServer]
    D --> D1{vscode built-in? vscode_/terminal_/_confirmation}
    D1 -- yes --> R1[vscode-tools]
    D1 -- no --> D2{matches a configured server id? substring/path/hyphen}
    D2 -- yes --> R2[serverId]
    D2 -- no --> D3{starts with mcp_<id>_ ?}
    D3 -- yes --> R3[extracted serverId]
    D3 -- no --> R4[other-tools]
    R1 --> E[group into serverMap]
    R2 --> E
    R3 --> E
    R4 --> E
    E --> F[map -> MCPServer with formatted names + tools]
```

## 4. getMcpConfigPath — profile-aware resolution 🟢

```mermaid
flowchart TD
    A([getMcpConfigPath]) --> B[getVSCodeUserDataPath - appName -> IDE dir, +WSL/darwin/win]
    B --> C{profiles/ dir exists?}
    C -- yes --> D[scan each profile for mcp.json + mtime]
    D --> E{any found?}
    E -- yes --> F[return newest-modified profile mcp.json]
    E -- no --> G
    C -- no --> G{User/mcp.json exists?}
    G -- yes --> H[return User/mcp.json]
    G -- no --> I[return User/mcp.json path anyway - for future creation]
```

## 5. checkCLI — version probe with extended PATH 🟢

```mermaid
flowchart TD
    A([checkCLI command, timeout]) --> B[execAsync with getExtendedPath + timeout]
    B -->|ok| C{output parses as JSON with version?}
    C -- yes --> C1[installed:true, version=json.version]
    C -- no --> C2[extractVersion regex -> installed:true]
    B -->|error| D{killed/SIGTERM?}
    D -- yes --> D1[installed:false, error=timeout]
    D -- no --> E[locateCLIExecutable via which/where]
    E --> E1{found?}
    E1 -- yes --> E2[installed:false + captured version/output]
    E1 -- no --> E3[installed:false, error]
```

## 6. sendPromptToChat — decorate + dispatch 🟢

```mermaid
flowchart TD
    A([sendPromptToChat prompt, ctx, files]) --> B[buildFinalPrompt]
    B --> B1[+ global custom instruction]
    B1 --> B2[+ instructionType-specific instruction]
    B2 --> B3{chatLanguage != English?}
    B3 -- yes --> B4[+ "Please respond in <lang>"]
    B3 -- no --> C
    B4 --> C{injected ChatDispatcher?}
    C -- yes --> D[dispatcher.dispatch finalPrompt, {specId}, files]
    C -- no --> E{files & VS Code >= 1.95.0?}
    E -- yes --> F[workbench.action.chat.open {query, files}]
    E -- no --> G[workbench.action.chat.open {query}]
```
