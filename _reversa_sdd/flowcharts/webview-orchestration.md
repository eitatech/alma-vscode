# Flowcharts — `webview-orchestration` module

> Source: `ui/src/features/{orchestration,workflow-composer}/`, `ui/src/components/{workflow,workflow-graph,kanban,devin,cloud-agents}/`, `ui/src/stores/{devin-store,cloud-agent-store}.ts`. Confidence: 🟢 CONFIRMED unless noted.
> Generated by the Reversa Archaeologist (escavação phase).
> Groups the agent-orchestration / monitoring webview surfaces. ⚠️ This module is a **layered prototype graveyard**: only the `orchestration` and `workflow-composer` pages are registered in `page-registry.tsx`. The Kanban board and the legacy Devin / Cloud-Agent progress views are **built but not reachable** through the current page registry (see §4).

## 1. Orchestration view: ready → snapshot → bucketed lanes 🟢

> `features/orchestration/index.tsx`. Registered page `"orchestration"` (host: `providers/orchestration-view-provider.ts`).

```mermaid
flowchart TD
    A([OrchestrationFeature mounts]) --> B[addEventListener message<br/>+ postMessage orchestration/ready]
    B --> C{message.type == orchestration/snapshot?}
    C -- no --> C
    C -- yes --> D[setSnapshot payload ?? EMPTY_SNAPSHOT<br/>setIsLoading false]
    D --> E[group sessions by bucket<br/>active / waiting / completed / failed]
    E --> F{sessions.length == 0?}
    F -- yes --> G[derive EmptyState from degradedReasons<br/>+ provider availability]
    G --> G1[render EmptyState + optional action button]
    F -- no --> H[render 4 PanelSection lanes<br/>each = StatusBadge count + SessionCard list]
    H --> I{user action on SessionCard}
    I -- Open session --> J[postMessage orchestration/open-session sessionId]
    I -- Open original view --> K[postMessage orchestration/open-existing-surface source]
    I -- Open external --> L[postMessage orchestration/open-external url]
    H --> M{header action}
    M -- Refresh --> N[postMessage orchestration/refresh]
    M -- Open Agent Chat / Cloud Agents --> K
```

## 2. Empty-state decision tree (degraded-mode messaging) 🟢

> `features/orchestration/index.tsx:112-217` — `useMemo` over `degradedReasons` + provider state. First match wins.

```mermaid
flowchart TD
    A([compute emptyState]) --> B{degradedReasons includes<br/>'provider wiring is unavailable'?}
    B -- yes --> B1[Cloud orchestration temporarily unavailable<br/>-> Open Agent Chat]
    B -- no --> C{includes 'No cloud agent providers are registered'?}
    C -- yes --> C1[Connect a cloud provider -> Open Cloud Agents]
    C -- no --> D{includes 'could not be read' / 'Failed to read'?}
    D -- yes --> D1[Session status temporarily unavailable -> Refresh state]
    D -- no --> E{registryAvailable AND providerCount>0 AND no activeProvider?}
    E -- yes --> E1[No cloud provider selected -> Open Cloud Agents]
    E -- no --> F{registryAvailable AND providerCount==0?}
    F -- yes --> F1[No sessions yet -> Open Agent Chat]
    F -- no --> G[No running or recent sessions -> Open Agent Chat]
```

## 3. Workflow Composer: hooks → React Flow graph (event → condition → schedule → action) 🟢

> `features/workflow-composer/index.tsx` + `utils/mapper.ts` + `components/workflow-graph/*`. Registered page `"workflow-composer"`. Reuses `hooks-view` types + `HookForm` (cross-module coupling to `webview-hooks-view`).

```mermaid
flowchart TD
    A([WorkflowComposerFeature mounts]) --> B[postMessage hooks/ready + hooks/list<br/>command = type with / -> .]
    B --> C{message hooks/sync | hooks.sync?}
    C -- yes --> D[setHooks payload.hooks]
    D --> E[mapHooksToGraph hooks]
    E --> F[setNodes / setEdges]
    F --> G[ReactFlow renders custom nodeTypes<br/>Source / Condition / Schedule / Action]
    G --> H{onNodeClick}
    H -- has data.hookId --> I[setSelectedHookId -> open side panel HookForm]
    I --> J{HookForm submit}
    J -- selectedHookId set --> K[postMessage hooks/update id, updates]
    J -- new --> L[postMessage hooks/create payload]
    K --> C
    L --> C

    subgraph MAP[mapHooksToGraph — deterministic layout]
      direction TB
      M1[per hook: lane y = hookIndex * 150 * 3] --> M2[processEvents: one source node per event<br/>x=100]
      M2 --> M3[processConditions: x+=300, chain from prev ids]
      M3 --> M4[processSchedule: skip if immediate, else x+=300]
      M4 --> M5[action node at final x; connectEdges from prev ids]
    end
```

## 4. ⚠️ Unmounted prototypes — Kanban board + legacy Devin/Cloud progress views 🔴

> Architectural finding. `KanbanBoard` has **no importer** in `ui/src` or `src`. The Devin/Cloud-Agent progress panels (`src/panels/devin-progress-panel.ts`, `src/panels/cloud-agent-progress-panel.ts`) request webview pages `"devin-progress"` / `"cloud-agent-progress"` that are **absent from `page-registry.tsx`** → the SPA renders "Unknown page".

```mermaid
flowchart LR
    subgraph LIVE[Reachable via page-registry]
      O[orchestration page] --- WC[workflow-composer page]
    end
    subgraph DEAD[Built but NOT registered]
      K[KanbanBoard<br/>6 columns by execution.state<br/>imports src/features/tasks/task-model]
      DP[DevinProgressView + devinStore<br/>spec 001 — singleton + useSyncExternalStore]
      CP[CloudAgentProgressView + createCloudAgentStore<br/>spec 016 'replaces Devin panel']
    end
    PANEL1[devin-progress-panel.ts] -. requests page 'devin-progress' .-> X[(not in registry)]
    PANEL2[cloud-agent-progress-panel.ts] -. requests page 'cloud-agent-progress' .-> X
    DP -. superseded by .-> CP
    CP -. superseded by .-> O
```

## 5. Kanban board grouping (logic, even if unmounted) 🟢

> `components/kanban/kanban-board.tsx` — pure grouping of `NormalizedTask[]` (compile-time import from `src/features/tasks/task-model`).

```mermaid
flowchart TD
    A[tasks: NormalizedTask array] --> B[for each ExecutionState column:<br/>queued, ready, running, blocked, completed, failed]
    B --> C[filter tasks where task.execution.state == column.state]
    C --> D[KanbanColumn renders KanbanCard list]
    D --> E[KanbanCard shows: title, priority badge,<br/>source.system, agent suggestedRole,<br/>started/completed times, dependsOn blockers,<br/>errorMessage, intent activity]
    E --> F{onClick / Enter / Space}
    F --> G[onTaskClick task -> host]
```
