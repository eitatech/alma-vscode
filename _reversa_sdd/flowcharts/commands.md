# Flowcharts — `commands` module

> Source: `src/commands/`. Confidence: 🟢 CONFIRMED unless noted.
> Generated by the Reversa Archaeologist (escavação phase).

## 1. handleStartNew — concurrent-cap enforcement + launch 🟢

```mermaid
flowchart TD
    A([gatomia.agentChat.startNew params]) --> B[enforceConcurrentCap]
    B --> B1{concurrentCap & checkCapacity wired?}
    B1 -- no --> OK[proceed]
    B1 -- yes --> B2[capacity = checkCapacity acp, cap]
    B2 --> B3{capacity.ok?}
    B3 -- yes --> OK
    B3 -- no --> B4{promptForCap wired?}
    B4 -- no --> B5[telemetry abort: no-prompt-helper] --> STOP([return])
    B4 -- yes --> B6[decision = promptForCap idleSessions, cap]
    B6 --> B7{decision.kind}
    B7 -- abort --> B8[telemetry abort] --> STOP
    B7 -- cancel-and-start --> B9[cancel idle runner] --> OK
    B7 -- cancel-only --> B10[cancel idle runner] --> STOP
    OK --> C[startAcpSession params -> session, runner]
    C --> D[registry.registerSession]
    D --> E[registry.attachRunner]
    E --> F[createPanel session]
    F --> G[registry.attachPanel - SINGLE SOURCE OF TRUTH]
    G --> H[panel.reveal] --> Z([open])
```

## 2. handleOpenForSession — restart fallback + one-panel reuse 🟢

```mermaid
flowchart TD
    A([openForSession id]) --> B[session = registry.getSession id]
    B --> C{found?}
    C -- no --> D[session = store.getSession id]
    D --> D1{found in store?}
    D1 -- no --> Z0([return])
    D1 -- yes --> E[registry.registerSession - lazy hydrate T047]
    C -- yes --> F
    E --> F{registry.focusPanel id?}
    F -- yes --> G[telemetry PANEL_REOPENED] --> Z0
    F -- no --> H[createPanel -> attachPanel -> reveal] --> Z([open])
```

## 3. handleChangeModel — ACP set_model with legacy fallback 🟢

```mermaid
flowchart TD
    A([changeModel sessionId, modelId]) --> B[resolve session registry||store]
    B --> C{found & modelId changed?}
    C -- no --> Z([return])
    C -- yes --> D[tryAcpSetModel]
    D --> D1{source==acp && manager?}
    D1 -- no --> F[fallback]
    D1 -- yes --> D2[manager.setSessionModel]
    D2 -->|ok| OK([done - manager fires models-changed])
    D2 -->|err includes ACP_NOT_SUPPORTED| F
    D2 -->|other err| THROW[re-throw to error boundary]
    F --> G[runner.recordModelChange? ]
    G --> H[store.updateSession selectedModelId] --> OK
```

## 4. Two-step worktree cleanup 🟢

```mermaid
flowchart TD
    A([cleanupWorktree sessionId, confirmedDestructive]) --> B{worktreeService?}
    B -- no --> E0[error: not configured]
    B -- yes --> C[resolve session + worktree]
    C --> C1{has worktree?}
    C1 -- no --> E1[error]
    C1 -- yes --> D[worktreeService.cleanup worktree, confirmedDestructive]
    D -->|ok| U[store.updateSession worktree.status=cleaned] --> R0([ok])
    D -->|WorktreeCleanupWarningRequired| W[warning: inspection] --> UI[UI shows 2-step dialog -> re-invoke confirmedDestructive=true]
    D -->|other err| R1([error: message])
```

## 5. Cloud dispatchTask — guard + retry 🟢

```mermaid
flowchart TD
    A([gatomia.dispatchTask item|taskId]) --> B[ensureActiveProvider]
    B --> B1{provider selected & credentialed?}
    B1 -- no --> B2[chain selectProvider / configureProvider] --> Z0([return])
    B1 -- yes --> C[extractTaskFromItem]
    C --> C1{task found?}
    C1 -- no --> E0[error: no task]
    C1 -- yes --> D{isTaskAlreadyRunning specTaskId?}
    D -- yes --> D1[warn: Open Session / Cancel] --> Z0
    D -- no --> F[build SpecTask + SessionContext - git branch/remote/featurePath]
    F --> G[createSessionWithRetry]
    G --> G1{attempt <= 2}
    G1 --> G2[provider.createSession]
    G2 -->|ok| H[storage.create]
    G2 -->|ProviderError.recoverable & attempts left| G3[backoff 1000*(n+1)ms] --> G1
    G2 -->|non-recoverable / exhausted| ERR[throw -> showErrorMessage]
    H --> I{pollingService running?}
    I -- no --> J[pollingService.start 30000]
    I -- yes --> K[onSessionCreated + onRefresh]
    J --> K --> Z([dispatched])
```

## 6. New-session QuickPick (tier-grouped) 🟢

```mermaid
flowchart TD
    A([gatomia.agentChat.newSession]) --> B[listProviders]
    B --> C[buildQuickPickItems: bucket into Installed / Available via npx / Install required + separators]
    C --> D[showQuickPick]
    D --> E{picked & providerId?}
    E -- no --> Z0([return])
    E -- yes --> F[re-resolve provider against original list]
    F --> G{install-required?}
    G -- yes --> H[openInstallUrl env.openExternal] --> Z0
    G -- no --> I[showInputBox: task instruction]
    I --> J{provided?}
    J -- no --> Z0
    J -- yes --> K[startNew agentId, displayName, taskInstruction] --> Z([session starting])
```
